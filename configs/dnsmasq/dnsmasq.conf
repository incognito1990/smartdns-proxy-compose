# Logging & upstreams
log-queries
no-resolv
server=8.8.4.4
server=1.1.1.1

# ---- Redirects (sanitize: no personal IPs) ----
# OpenAI / ChatGPT (и другие группы доменов; подставьте нужный IP локально)
address=/chatgpt.com/<REDIRECT_IP>
address=/api.openai.com/<REDIRECT_IP>
# ... остальной список из вашего файла, все с <REDIRECT_IP> ...
